import "package:flutter/material.dart";
import "package:patinka/swatch.dart";

const Color transparent = Color.fromRGBO(0, 0, 0, 0);
Color layerOneBg = swatch[800]!.withAlpha(100);
Color layerTwoBg = swatch[900]!.withAlpha(120);

Color forgotPasswordText = swatch[701]!;
Color signInButton = swatch[800]!.withAlpha(150);
Color signInText = swatch[501]!;

Color checkbox = swatch[801]!;
Color signInBox = swatch[601]!;

Color hintText = swatch[401]!.withAlpha(130);
Color inputBorder = swatch[701]!;

Color labelColour = swatch[101]!;
Color titleColour = swatch[501]!;
