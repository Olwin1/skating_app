import 'package:flutter/material.dart';

class Testingt extends StatelessWidget {
  const Testingt({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Welcome to Flutterrrreerr',
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Welcome to Fluttereeeeeeeeeeee'),
        ),
        body: const Center(
          child: Text('Hello World'),
        ),
      ),
    );
  }
}
